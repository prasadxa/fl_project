## 2024-05-18 - Added focus-visible states
**Learning:** Found that this vanilla SPA lacked consistent focus indicators, especially for keyboard users navigating buttons and inputs. The app relies on raw CSS without a framework.
**Action:** When working on this app, always add `:focus-visible` states explicitly since there's no reset/framework doing it automatically.
